import yaml from './example.yaml';
import toml from './example.toml';
import json from './example.json';

const { log } = console;

log('YAML:');
log(yaml);

log('TOML:');
log(toml);

log('JSON:');
log(json);
